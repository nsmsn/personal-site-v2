---
layout: layouts/home.njk
---
<div>
<div class="row flow">
<div class="grid-fourth grid-last flow">
<div class="bg-color-cycle img-circle" style="max-width:240px">
        <img src="/img/misc/victory-grain.png" alt="a hand with two extended fingers flashing the V for Victory or peace sign" width="240" class="img-circle img-multiply"/>
        </div>
</div>
  <div class="grid-three-fourths flow">
    <h1 class="home-heading">
      <script language="JavaScript">
        var myDate = new Date();
        if ( myDate.getHours() >= 5 && myDate.getHours() < 12 ) { document.write("Good morning."); }
        else if ( myDate.getHours() >= 12 && myDate.getHours() <= 17 ) { document.write("Good afternoon."); }
        else if ( myDate.getHours() > 17 && myDate.getHours() <= 22 ) { document.write("Good evening.");
        } else { document.write("Hello."); }
      </script>
      <noscript>Howdy.</noscript>
    </h1>
    <p class="lede">My name is Nick, which means <mark class="mark-highlight"><em>“<a href="https://www.behindthename.com/name/nicholas" title="Behind the Name">victory of the people</a>.”</em></mark> I&nbsp;design things for screens (mostly), paper (sometimes), and humans&nbsp;(always).</p>

    &nbsp;

    <div class="h-card vcard p-author">
    <a href="https://nicksimson.com" class="u-url u-uid" rel="author"><img src="/img/misc/grainy-nick-profile.png" alt="" width="80" height="80" class="img-circle u-photo hcard-photo vis-hidden"><span class="p-name vis-hidden">Nick Simson</span></a>
    <p><span class="p-note">I live in Knoxville, Tenn. and work as a graphic / communication / interaction designer.</span> I’m <strong class="p-nickname">@nsmsn</strong> on <a rel="me" href="https://github.com/nsmsn" class="u-url">Github</a>, <a rel="me" href="https://mastodon.design/@nsmsn" class="u-url">Mastodon</a>, and <a href="/links">a few other places</a>. You can learn more <a href="/info/">about&nbsp;me</a> or see what I’m up to&nbsp;<a href="/now/">now</a>.</p>
    </div>
    &nbsp;

    <h2>Lately...</h2>
      <div class="panel">
      {% set maxPosts = collections.posts.length | min(1) %}
      <p>You can read my latest {% if maxPosts == 1 %}post{% else %}{{ maxPosts }} posts{% endif %}:</p>

      {% set postslist = collections.posts | head(-1) %}
      {% set postslistCounter = collections.posts | length %}
      {% include "postslist.njk" %}

      <p>You’ll find plenty more to read in <a href="{{ '/posts/' | url }}">the archive</a>.</p>
 
  </div>
</div>
</div>